#pragma once
#include <vector>
using namespace std;

class TwoSumII{
public:
    int BinarySearch(vector<int>& nums, size_t start, size_t end, int target);
    vector<int> twoSum(vector<int>& numbers, int target);
};

